import { Platform } from 'react-native';

// Database file name used across mobile and server
// export const DB_NAME = 'questions.db';

// Server-side database paths (only used in Node environment)
// export const DB_DIR = Platform.OS === 'web' ? 'data' : '';
// export const DB_PATH = Platform.OS === 'web' ? `${DB_DIR}/questions.db` : '';

// Mobile app paths
export const DB_PATH = `../assets/database/questions.db`;
// export const MOBILE_DB_DIR = 'SQLite';
